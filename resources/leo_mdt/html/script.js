// In NUI MapPanel, ensure clicking a marker requests host detail
// Add marker.on('click') to request host detail via fetch

// Also add handling for hostAlert and permissionDenied in handleMessage

// Locate handleMessage in script.js and add two cases: hostAlert and permissionDenied

